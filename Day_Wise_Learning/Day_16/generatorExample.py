def is_prime():
    pass

def prime_generator(limit=10):
    count,i=1,1
    while count<=limit:
        start+=1
        if is_prime (start) :
            count+=1
            yield start



        
